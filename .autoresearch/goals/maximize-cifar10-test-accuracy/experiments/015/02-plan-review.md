1. **[Verification Protocol steps 3-4 / `preflight.py`]** The plan never requires the semantic and throughput harness to execute the candidate loss branch that the scored `main()` actually uses. Importing and calling `train.mixup_batch` can validate coefficient generation and pixel interpolation, while a separately reimplemented elementwise loss in `preflight.py` can pass alignment, gradient, and timing checks even if lines 231-238 of `train.py` use the vector incorrectly. Require the candidate benchmark/semantic path to exercise shared production code for both mixing and loss construction, or add an explicit source/runtime instrumentation check of one real `main()` mixup step; otherwise the strongest preflight gates can certify a correct copy rather than the scored implementation.

2. **[Milestone 2 / Verification Protocol step 2 scope gate]** `git diff --name-only eb08811 --` does not report untracked or ignored files, yet the plan creates an ignored Python harness and Python can import other untracked files from the project root. Thus “require exactly `train.py`” does not actually establish that no additional local code can influence the scored run. Add a `git status --short --untracked-files=all`/ignore audit with an explicit allowlist for the experiment artifact, and confirm no untracked root-level modules or package files are present before scoring.

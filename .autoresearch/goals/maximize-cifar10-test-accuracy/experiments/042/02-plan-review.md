# Adversarial Plan Review EXP-042

No execution blocker found. Apply these hardening corrections before execution:

1. **[Code Changes / final pooling] The production `bmm` contract is not executable as written.** Line 23 says `bmm(features, attention_delta)` after only saying to flatten features; `torch.bmm` requires two rank-three tensors and the result must be `[B,128]` before it can be added to `mean_pooled`. Specify the proposal's exact shapes and operations in the plan: `spatial_features = out.flatten(2)` (`[B,128,64]`), `score_logits = self.pool_score(out).flatten(1)` (`[B,64]`), and `pooled_correction = torch.bmm(spatial_features, attention_delta.unsqueeze(2)).squeeze(2)` (`[B,128]`). This also fixes which axis softmax and the independent direct-weighted oracle must use.

2. **[Verification 6-7 / covariance and common gradients] Batch/loss scaling is explicit only for the synthetic sum objective, not for production CE.** For `(z*g).sum()`, `sum_b Cov_population(X_b) @ g_b` is correct; for mean-reduced CE/mixup, the same expression is correct only when `g_b` is defined as the already mean-scaled autograd value `dL_mean/dz_b`. If the oracle instead derives per-example unreduced CE gradients, it must divide the batch sum by `B` (and apply both mixup coefficients). State this distinction and test at least one mean-reduced objective, or a factor-of-256 error can survive the current synthetic oracle plus mere "nonzero" full-model check.

3. **[Verification 3 / optimizer identity] "Accepted optimizer membership/order exact" contradicts the required new scorer parameter.** Compare the ordered common-parameter subsequence by name in each group, require the no-decay group to be identical, and require the decay group to equal the accepted ordered list plus exactly `pool_score.weight` once at its registration position. Then compare all group options. Comparing raw memberships for equality must fail; ignoring membership could miss duplicate or wrong-group scorer registration.

4. **[Verification 5 / spatial roll] The pooled result is translation invariant, not equivariant.** Under a matching cyclic roll of the `[B,128,8,8]` fixture, `score_logits`, `attention`, and centered coefficients must roll equivariantly, while `mean_pooled`, the centered correction, final pooled vector, and logits must remain invariant within predeclared dtype tolerances. The current phrase "verify translation equivariance" can assert the wrong property or become a vacuous shape-incompatible check.

5. **[Verification 4-7 / startup and gradient identity] The FP32 comparison bounds and common-gradient mapping are deferred to the harness.** Predeclare numeric CPU/CUDA `rtol`/`atol` values before observing errors for nonzero direct-pool equivalence and common gradients. Compare common gradients by parameter name, including shape/dtype, `None` status, and maximum absolute/relative error; exclude only `pool_score.weight`. Keep the stronger bitwise requirements for zero scores, centered coefficients, correction, accepted pooled features/logits, common state bytes, and RNG. Otherwise "tight declared tolerance" can be adjusted after results or can hide a missing common gradient.

6. **[Verification 8 / update oracle] The all-parameter oracle needs an explicit state scope and post-update attention fixture.** Require the formula for every named trainable parameter using that parameter's actual group LR and `wd` (`5e-4` iff `ndim>=2`, otherwise zero), compare fresh/preseeded momentum-buffer existence, dtype, value, and parameter value, and separately verify non-parameter BN buffers evolve only through the forward and are not touched by SGD. Compute post-update scores/attention on the same fixed pre-update input after applying the independently predicted full update; report early and hard fixtures separately. This prevents a scorer-only oracle or a check that silently skips rank-one/no-decay tensors.

7. **[Verification 9-10 and 12 / timing and closure] Define retained timing samples precisely and separate evidence from search policy.** For each implementation and regime, make each of the four retained samples the mean seconds/step of one >=50-step complete window; compute the median and population CV over those four window means, warm each implementation/regime for >=20 disposable steps, restore the same fixture before every retained window, and reset/synchronize peak-memory accounting around candidate windows. Use those four-sample medians in the stated harmonic time-fraction retention formula. Also change line 59 so a normal-exposure miss evidentially rejects only the exact zero-started, temperature-one, one-query treatment; temperature/init/scale/query-count/cutoff variants are declined as preregistered post-result search policy, not experimentally rejected.

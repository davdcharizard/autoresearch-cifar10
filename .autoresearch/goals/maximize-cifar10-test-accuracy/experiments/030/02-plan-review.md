1. **[Milestone 4 / Verification Procedure 9] The endpoint corroboration is incorrectly promoted into a second objective gate.** Lines 24-26 and 64 say to reject a run unless `final_test_acc >=94.32%`, even when the valid fixed-budget run has `best_test_acc >=94.42%`. `TASK.md` and `01-definition.md` define only `best_test_acc` as the objective/necessary improvement metric, and `01-idea-review.md:11` explicitly says endpoint corroboration does not replace that metric. This rule can therefore discard a genuine objective improvement. Keep the preregistered final accuracy and loss as a separate mechanism-corroboration verdict, while classifying goal improvement from the primary metric and hard constraints.

2. **[Milestone 3 / Abort Criteria / Verification Procedure 7] A realized exposure miss is classified contrary to the approved one-run rule.** Lines 21, 50, and 62 make `<130` realized passes a failed/invalid score, but `experiments/028/proposals/idea-03.md:210-216` says such a completed score still counts, cannot be rerun, and is operationally inconclusive. Exposure is an experiment-specific preflight/hypothesis gate, not a task validity constraint. The plan must preserve and classify any otherwise valid sole score by its primary metric, prohibit a rerun, and report `<130` separately as failure to establish the targeted mechanism under the intended operating regime.

3. **[Milestone 2 / Code Changes / Verification Procedure 4] The claimed production transition proof has no executable oracle.** The drop-path mutation remains inline in `main()`, while the plan only promises helper tests plus unspecified "transition source/runtime behavior"; directly setting `layer3[2]` to zero in the harness cannot prove that production does it exactly once, before the first hard-label forward, or never re-enables it. The extra `DROP_PATH_END_FRACTION` also appears behaviorally unused because the transition is tied to `use_mixup`, so checking that constant would not prove the actual cutoff. Specify either an extracted controller that production and the harness both invoke, or an instrumented execution of the production loop that traces the predicate, mutation, and forward ordering; remove the redundant cutoff or assert its equality at the actual control point.

4. **[Milestone 2 / Verification Procedure 5] The weighted timing ratio is not the exact fixed-time exposure ratio asserted.** For fixed 195-second and 105-second phases, total steps are proportional to `0.65 / early_ms + 0.35 / hard_ms`; the proposed ratio `(0.65*accepted_early_ms + 0.35*accepted_hard_ms) / (0.65*candidate_early_ms + 0.35*candidate_hard_ms)` is generally different whenever early and hard step times differ. Because `0.9774` is a sharp pass/fail gate derived to four decimals, use `(0.65/candidate_early_ms + 0.35/candidate_hard_ms) / (0.65/accepted_early_ms + 0.35/accepted_hard_ms)` (or a conservative bound) before multiplying by 133.00736.

1. **Critical - Milestone 1 / Verification Procedure steps 2-3:** The planned diff audit constrains only the changed filename and manually checks a few optimizer tokens; it does not verify that the rest of `train.py` is unchanged. Arbitrary edits to the seed, data path, evaluation/reporting logic, model, or training loop would still pass `git diff --name-only eb08811` and the stated spot checks, defeating attribution and leaving a reward-hacking path through metric reporting. Require review of the complete `git diff eb08811 -- train.py` against an explicit four-change allowlist: add `WARMUP_START_LR`, set `MIN_LR` to zero, substitute the warmup formula's floor term, and substitute the optimizer's initial LR; reject any other hunk.

2. **High - Verification Procedure step 3:** `learning_rate` accepts counted seconds, but the plan specifies calls only as percentages and never states the required conversion `learning_rate(progress * TIME_BUDGET_S)`. Calling it with `0.65`, for example, tests 0.217% rather than 65%. Moreover, the listed points check only the two warmup endpoints; 5% enters the cosine branch, so they do not prove the claimed *entire* 0-5% warmup path is preserved. Make the assertion command explicit in seconds and add at least the 2.5% interior value (`0.101`) plus a value immediately below 5% to catch a wrong warmup formula or boundary.

3. **Medium - Milestone 3 / Verification Procedure step 5:** `training_seconds approximately 300` has no numerical acceptance range, so a run that terminates materially early at `MAX_STEPS` could be accepted by subjective interpretation. Pre-register a tight machine-checkable interval that allows the single-step overshoot and one-decimal log rounding while rejecting early termination (for example, require the reported value to be `300.0`, together with `num_steps < MAX_STEPS`), and separately require the run log's `Device: cuda` line so the H20 inventory check is tied to the actual execution device.

# Brainstorm EXP-054
## Chosen: torch.manual_seed(0) with best config
Different torch seed = different weight init + data shuffle. A fundamentally different training trajectory that could converge higher.

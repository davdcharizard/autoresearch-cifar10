# Navigation - Experiment 009

## Search Intent

Explore a new representation branch after four distinct failed children accumulated at the global-best tip, while retaining the validated WRN and CutMix foundation.

## Chosen Base
**002** on `br-000` @ 95.23% - explore

EXP-002 preserves the strongest architecture and augmentation mechanisms before late SAM. Identity-preserving ECA can therefore be tested as a clean additive representation change without two-pass integration or step loss. If it clears the local 95.33% gate, the successful EXP-004 SAM package remains an evidence-backed follow-up that can be composed on the new branch.

## Alternatives Considered

- **004** - global best at 95.40%, but already has four failed children spanning data history, mixed-sample regularization, optimizer geometry, and CPU augmentation; adding a fifth sibling underweights the policy's failed-child signal.
- **001** - cleaner architecture-only base, but unnecessarily discards the validated +0.61-point CutMix mechanism, which is compatible with deterministic channel attention.
- **BASE** - too much validated progress would be discarded for a small additive module.

## Policy Influence

The policy explicitly weighs failed-child pileups. Four terminal children at EXP-004 now justify an interior-node fork, while EXP-002 is the nearest successful ancestor that isolates ECA and preserves most lineage momentum.

# Navigation - Experiment 015

## Search Intent

Exploit the global-best recipe with a mechanism orthogonal to the three failed children, prioritizing negligible-overhead changes that preserve optimizer and EMA exposure.

## Chosen Base
**011** on `br-000` @ 95.61 - exploit

EXP-011 remains the only branch tip and global best, with a stable full-state EMA improvement and 25,798-step exposure. Its failed children tested complementary erasure, fixed-scale cosine geometry, and a width-320 point rejected on latency; none tests the already developed soft-target loss-shaping or corrected trajectory-averaging directions (`tree.sh children 011`; EXP-012 through EXP-014 reports).

## Alternatives Considered
- 004 - Forking before EMA would surrender a validated +0.21-point, negligible-overhead mechanism without evidence that EMA conflicts with the next low-cost idea.
- 002 - A larger fork would also remove validated SAM and EMA gains; its prior failed architecture and channel-gating children do not improve the expected payoff of restarting there.

## Policy Influence

The policy's failed-child pileup prompted an explicit saturation check. Three failures at EXP-011 are material but mechanism-diverse, while untried low-overhead directions remain and the lineage still has the strongest metric and tail characterization, so exploitation retains the best expected payoff.
